---
interface Props {
	class?: string;
	id?: string;
}

const { class: className, id } = Astro.props;
---

<section id={id} class:list={["px-4 w-full flex justify-center", className]}>
	<div class="max-w-7xl w-full">
		<slot />
	</div>
</section>
