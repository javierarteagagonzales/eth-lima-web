---
interface Props {
	className?: string;
	color?: "black" | "white" | "brand";
}

const { className = "h-8" } = Astro.props;
const base = import.meta.env.BASE_URL;
---

<img 
	src={`${base}logo/EthLima.webp`} 
	alt="ETH Lima Logo" 
	class:list={[className, "w-auto object-contain"]}
/>
