---
import SectionContainer from "./SectionContainer.astro";

interface Props {
	title: string;
	description: string;
	class?: string;
}

const { title, description, class: className } = Astro.props;
---

<SectionContainer class:list={["bg-neutral-100 min-h-screen flex items-center justify-center text-center pt-40 pb-24", className]}>
	<div class="max-w-3xl mx-auto flex flex-col gap-6">
		<h2 class="text-3xl md:text-5xl font-bold text-primary leading-tight tracking-tighter">
			{title}
		</h2>
		<p class="text-lg md:text-2xl text-neutral-600 font-medium leading-relaxed">
			{description}
		</p>
	</div>
</SectionContainer>
